# KLCP Practice Exam — 200 Multiple Choice Questions

---

## Questions

---

**1.** What is the direct predecessor of Kali Linux?

A) WHoppiX  
B) WHAX  
C) BackTrack  
D) Auditor Security Linux  

---

**2.** Kali Linux 1.0 was released in which year?

A) 2011  
B) 2012  
C) 2013  
D) 2014  

---

**3.** Kali Linux is built on top of which base distribution?

A) Ubuntu  
B) Fedora  
C) Debian  
D) Arch Linux  

---

**4.** Who is credited as the original creator of WHoppiX and a key founder of Kali Linux?

A) Jim O'Gorman  
B) Devon Kearns  
C) Mati Aharoni  
D) Raphaël Hertzog  

---

**5.** BackTrack versions prior to BackTrack 4 were based on which distribution?

A) Debian  
B) Ubuntu  
C) SLAX (Slackware)  
D) Fedora  

---

**6.** BackTrack 4 marked a significant evolution because it:

A) Became based on Fedora  
B) Gained an update mechanism via Ubuntu repositories  
C) Was released as a live CD only  
D) Dropped wireless support  

---

**7.** In how many days did Kali Linux 1.0 reach 90,000 downloads?

A) 1  
B) 3  
C) 5  
D) 7  

---

**8.** Kali Linux synchronizes with Debian repositories how many times per day?

A) Once  
B) Twice  
C) Three times  
D) Four times  

---

**9.** Which of the following correctly describes Kali Linux's current release model?

A) Fixed annual releases  
B) Rolling distribution  
C) Long-Term Support (LTS) releases  
D) Semi-annual releases  

---

**10.** Which project opened the door for Kali Linux on mobile devices?

A) Kali Dojo  
B) NetHunter  
C) Kali ARM  
D) Kali CloudBuild  

---

**11.** The Linux kernel typically runs in which protection ring?

A) Ring 1  
B) Ring 2  
C) Ring 3  
D) Ring 0  

---

**12.** The term "user space" refers to:

A) RAM allocated to the kernel  
B) Everything happening outside of the kernel  
C) The /usr directory on disk  
D) Kernel-level processes only  

---

**13.** Hardware information detected by the Linux kernel is exported through which virtual file systems?

A) /dev/ and /bin/  
B) /proc/ and /sys/  
C) /etc/ and /var/  
D) /tmp/ and /run/  

---

**14.** Which letter in the output of `ls -l` indicates a block device?

A) c  
B) d  
C) b  
D) s  

---

**15.** Which type of device file does a serial port use?

A) Block device  
B) Socket device  
C) Character device  
D) Named pipe  

---

**16.** The `ioctl` system call is used for:

A) Reading files  
B) Device-specific commands  
C) Network socket creation  
D) Changing file permissions  

---

**17.** Which command is used to view a manual page for a specific command?

A) help  
B) info  
C) man  
D) doc  

---

**18.** In the `man` page numbering system, section 2 covers:

A) Configuration files  
B) System calls  
C) Library functions  
D) Games  

---

**19.** To find a man page by keyword when you don't know the command name, you use:

A) whereis  
B) locate  
C) apropos  
D) which  

---

**20.** Which section of the man pages covers system administration commands?

A) Section 5  
B) Section 7  
C) Section 8  
D) Section 9  

---

**21.** The default filesystem type proposed by the Kali installer is:

A) xfs  
B) btrfs  
C) ext3  
D) ext4  

---

**22.** Which guided partitioning method is most appropriate for servers and multi-user systems?

A) All files in one partition  
B) Separate /home/ partition  
C) Separate /home, /var, and /tmp partitions  
D) Full disk encryption only  

---

**23.** During Kali installation, the network is automatically configured using:

A) Static IP only  
B) DHCP (for IPv4/IPv6) and ICMPv6 Neighbor Discovery  
C) PPPoE  
D) Manual DNS entry  

---

**24.** What tool does the installer suggest for generating strong passwords?

A) openssl  
B) gpg  
C) pwgen  
D) pass  

---

**25.** Which NTP-related action happens automatically if the network is available during installation?

A) The system clock is permanently disabled  
B) The clock is synced from a network time server  
C) The timezone is set to UTC regardless of location  
D) NTP daemon is installed but not started  

---

**26.** In the MSDOS partition table, how many primary partitions are allowed?

A) 2  
B) 3  
C) 4  
D) 8  

---

**27.** Which partition must be a primary partition on an MSDOS partition table?

A) /home  
B) /var  
C) The partition containing /boot  
D) The swap partition  

---

**28.** LVM stands for:

A) Linux Volume Manager  
B) Logical Volume Manager  
C) Layered Virtual Mount  
D) Linux Virtual Memory  

---

**29.** LUKS is used for:

A) Package management  
B) Network configuration  
C) Full disk encryption  
D) Bootloader management  

---

**30.** To disable DHCP during installation and force a static IP, which boot parameter is used?

A) netcfg/static=true  
B) netcfg/use_dhcp=false  
C) network/dhcp=off  
D) ip=static  

---

**31.** The Kali installer creates a new user account that is in which group by default?

A) admin  
B) wheel  
C) sudo  
D) root  

---

**32.** After selecting "Guided - use entire disk," what action does the installer take?

A) Installs alongside existing OS  
B) Erases all data on the selected disk  
C) Creates only the swap partition  
D) Resizes the existing partition  

---

**33.** The "Separate /home/ partition" method benefits users because:

A) It speeds up boot time  
B) User data is preserved if the system is reinstalled  
C) It enables full encryption automatically  
D) It reduces disk usage  

---

**34.** In Kali Linux, which policy governs network services after installation?

A) All services are enabled by default  
B) Network services are disabled by default  
C) Only SSH is enabled  
D) Only HTTP is enabled  

---

**35.** Kali Linux Forensics mode prevents:

A) The keyboard from functioning  
B) Auto-mounting of detected disks  
C) Network card activation  
D) Package installation  

---

**36.** To enable a service manually in Kali Linux, you use:

A) service start servicename  
B) sudo systemctl enable service  
C) sudo /etc/init.d/service start  
D) sudo chkconfig service on  

---

**37.** Kali Linux's custom kernel includes patches for:

A) Bluetooth mesh support  
B) Wireless injection support  
C) GPU compute  
D) CPU frequency scaling  

---

**38.** Kali Linux firmware files are located in:

A) /usr/lib/firmware/  
B) /lib/firmware/  
C) /etc/firmware/  
D) /boot/firmware/  

---

**39.** Which ARM architectures does Kali provide binary packages for?

A) armv6, armv7, armv8  
B) armel, armhf, arm64  
C) arm32, arm64 only  
D) armel only  

---

**40.** Kali Linux's live-build configuration is published so that:

A) Users can contribute to the kernel  
B) Users can submit bug reports  
C) Users can customize the official images  
D) Developers can manage upstream Debian  

---

**41.** In the Kali tool categories, which category would you find tools like tcpdump and Wireshark?

A) Exploitation Tools  
B) Sniffing & Spoofing  
C) Post Exploitation  
D) Password Attacks  

---

**42.** Social Engineering Tools in Kali are used to:

A) Exploit binary vulnerabilities  
B) Exploit human behavior as an attack vector  
C) Crack WPA2 handshakes  
D) Analyze network traffic  

---

**43.** The `dpkg -L` option lists:

A) All installed packages  
B) Files installed by a specific package  
C) Packages available for installation  
D) Packages that require updates  

---

**44.** Which command finds which package owns a specific file?

A) dpkg -l /path/to/file  
B) dpkg -S /path/to/file  
C) dpkg -i /path/to/file  
D) dpkg -r /path/to/file  

---

**45.** The `dpkg -s` command:

A) Removes a package  
B) Searches for a file in all packages  
C) Displays headers of an installed package  
D) Lists all installed packages  

---

**46.** Where is the dpkg database stored on the filesystem?

A) /etc/dpkg/  
B) /usr/share/dpkg/  
C) /var/lib/dpkg/  
D) /lib/dpkg/  

---

**47.** To install a package with APT and automatically resolve dependencies:

A) dpkg -i package  
B) apt install package  
C) apt-get add package  
D) aptitude fetch package  

---

**48.** Which command fully removes a package including all configuration files?

A) apt remove package  
B) dpkg -r package  
C) apt purge package  
D) dpkg -d package  

---

**49.** When using `apt remove`, which files are retained?

A) Binary files only  
B) Configuration files and user data  
C) Nothing is retained  
D) Log files only  

---

**50.** To force overwrite of a conflicting file during dpkg installation:

A) dpkg -i --force-install  
B) dpkg -i --force-overwrite  
C) dpkg -i --override-files  
D) dpkg -i --conflict-skip  

---

**51.** The `apt full-upgrade` command differs from `apt upgrade` because it:

A) Only upgrades security packages  
B) Can remove obsolete packages or install new dependencies  
C) Upgrades the kernel only  
D) Runs with root privileges automatically  

---

**52.** `apt-get dist-upgrade` is equivalent to which modern apt command?

A) apt upgrade  
B) apt safe-upgrade  
C) apt full-upgrade  
D) apt fix-broken  

---

**53.** How often does Kali generally recommend upgrading the rolling distribution?

A) Daily  
B) At least once a week  
C) Once a month  
D) Only when a security advisory is issued  

---

**54.** Which APT configuration file line sets the default release target?

A) APT::Default-Target "kali-rolling";  
B) APT::Default-Release "kali-rolling";  
C) Apt::Release "kali-rolling";  
D) APT::Source "kali-rolling";  

---

**55.** The `apt-listchanges` package is useful for:

A) Listing available repositories  
B) Displaying information about possible problems before a package upgrade  
C) Listing installed packages  
D) Auditing installed package signatures  

---

**56.** To install a specific version of a package using apt:

A) apt install package:version  
B) apt install package=version  
C) apt install package@version  
D) apt install package#version  

---

**57.** To install a package from a specific distribution using apt:

A) apt install package~distribution  
B) apt install package/distribution  
C) apt install package:distribution  
D) apt install package@distribution  

---

**58.** The `--force-overwrite` option in APT is passed to dpkg using:

A) apt --dpkg-opts="--force-overwrite"  
B) apt -o Dpkg::Options::="--force-overwrite"  
C) apt --force dpkg-overwrite  
D) apt pass="--force-overwrite"  

---

**59.** What does the `dpkg --purge` command do compared to `dpkg --remove`?

A) It only removes config files  
B) It removes user data only  
C) It removes the package and all associated data including config files  
D) It is identical to remove  

---

**60.** Running `apt install package1 package2-` will:

A) Install both packages  
B) Install package1 and remove package2  
C) Remove package1 and install package2  
D) Remove both packages  

---

**61.** To recompile the Linux kernel as a Debian package, you run:

A) make kernel-deb  
B) make deb-pkg  
C) dpkg-buildkernel  
D) kernel-package build  

---

**62.** Which package must be installed to build a Debian kernel package?

A) kernel-dev  
B) build-essential  
C) make-tools  
D) linux-build  

---

**63.** The `libncurses5-dev` package is required for which kernel configuration interface?

A) make xconfig  
B) make gconfig  
C) make menuconfig  
D) make nconfig  

---

**64.** The `make oldconfig` command is used when:

A) You are compiling a kernel from scratch  
B) You have an outdated .config file from a different kernel version  
C) You want to use default settings  
D) You are running make distclean  

---

**65.** `make distclean` differs from `make clean` because it:

A) Only removes object files  
B) Also removes the .config file  
C) Only cleans driver modules  
D) Runs faster  

---

**66.** The kernel sources installed from the linux-source package are placed in:

A) /usr/src/linux/  
B) /usr/src/linux-source-version.tar.xz  
C) /boot/kernel-source/  
D) /lib/kernel/source/  

---

**67.** To copy the current kernel's config into the kernel source directory:

A) cp /boot/config-$(uname -r) ~/kernel/linux-source-*/.config  
B) cp /etc/kernel.conf ~/kernel/  
C) make getconfig  
D) cp /proc/config.gz ~/kernel/  

---

**68.** `make xconfig` requires which development package?

A) libgtk2.0-dev  
B) libqt4-dev  
C) libncurses5-dev  
D) libglade2-dev  

---

**69.** `linux-image-version` package generated by `make deb-pkg` contains:

A) Kernel header files  
B) The kernel image and associated modules  
C) Debugging symbols  
D) User-space C library headers  

---

**70.** If you have `*-dkms` packages installed, which kernel package must also be installed?

A) linux-firmware-image  
B) linux-image  
C) linux-headers  
D) linux-libc-dev  

---

**71.** Custom Kali Live ISO images are built using:

A) live-cd-build  
B) mkisofs  
C) live-build  
D) iso-creator  

---

**72.** The live-build configuration repository is hosted at:

A) github.com/kalilinux  
B) gitlab.com/kalilinux/build-scripts/live-build-config  
C) bitbucket.org/kalilinux  
D) sourceforge.net/kali  

---

**73.** To build a Kali ISO with KDE desktop environment:

A) ./build.sh --desktop kde  
B) ./build.sh --variant kde --verbose  
C) ./build.sh --env kde  
D) ./build.sh kde --verbose  

---

**74.** The `kali-linux-everything` metapackage:

A) Contains only the top 10 tools  
B) Includes all metapackages and almost every tool Kali provides  
C) Is the base system only  
D) Installs only command-line tools  

---

**75.** Which metapackage includes the default Kali installation with both CLI and graphical tools?

A) kali-linux-core  
B) kali-linux-headless  
C) kali-linux-default  
D) kali-linux-large  

---

**76.** Chroot hooks in live-build are executed:

A) Outside the chroot, after the ISO is finalized  
B) Within the chroot environment during build  
C) On first boot of the live system  
D) During the GRUB installation stage  

---

**77.** Binary hooks in live-build run:

A) Inside the chroot at build time  
B) At first system boot  
C) Outside the chroot at the end of the build process  
D) During the package download phase  

---

**78.** To add a file to the live filesystem using live-build, place it in:

A) includes.binary  
B) includes.chroot  
C) packages.chroot  
D) hooks/live/  

---

**79.** USB persistence is enabled when the boot parameters include:

A) usb-save  
B) persist-mode  
C) persistence  
D) save-data  

---

**80.** The Kali live image includes which two persistence-related boot menu entries by default?

A) Live USB Save and Live USB Encrypted Save  
B) Live USB Persistence and Live USB Encrypted Persistence  
C) Live USB Read-Only and Live USB RW  
D) Persistent Mode and Full Disk Mode  

---

**81.** The CIA triad in information security stands for:

A) Confidentiality, Investigation, Authorization  
B) Control, Integrity, Availability  
C) Confidentiality, Integrity, Availability  
D) Compliance, Inspection, Auditing  

---

**82.** Which CIA attribute is most critical for a medical prescription tracking system?

A) Availability  
B) Confidentiality  
C) Integrity  
D) Authentication  

---

**83.** A SQL injection that triggers a denial-of-service by consuming server resources impacts which CIA attribute?

A) Confidentiality  
B) Integrity  
C) Availability  
D) Authentication  

---

**84.** In risk management, "threat" is best described as:

A) The flaw that allows an attack  
B) Who or what would carry out the attack  
C) The likelihood of a vulnerability being exploited  
D) The impact of a successful attack  

---

**85.** In risk management, "vulnerability" is best described as:

A) Who would do it to you  
B) What allows the threat to cause harm  
C) The severity of the impact  
D) The organization's risk tolerance  

---

**86.** Which of the following is a type of vulnerability that allows writing to memory beyond allocated space?

A) SQL Injection  
B) Race Condition  
C) File Inclusion  
D) Buffer Overflow  

---

**87.** A race condition vulnerability is exploited by:

A) Injecting SQL commands  
B) Carefully timing actions to alter a sequence of events  
C) Including files from remote servers  
D) Overflowing stack memory  

---

**88.** Why is it recommended to start each assessment with a clean Kali installation?

A) To ensure maximum tool availability  
B) To prevent cross-contamination of client data and simplify maintenance  
C) Because Kali requires reinstallation after every reboot  
D) Because APT packages expire after each assessment  

---

**89.** The `kali-tools-forensics` metapackage, when booted in forensics mode, will:

A) Auto-mount all disks for analysis  
B) Use a swap partition to maximize speed  
C) Avoid auto-mounting disks or using swap  
D) Encrypt evidence automatically  

---

**90.** Adding a nuke password to a Kali encrypted installation allows you to:

A) Unlock the disk remotely  
B) Destroy the decryption key for safety during travel  
C) Bypass the LUKS passphrase  
D) Enable dual-boot mode  

---

**91.** WHoppiX was released in which year?

A) 2002  
B) 2004  
C) 2006  
D) 2008  

---

**92.** WHAX was based on which live CD system?

A) Knoppix  
B) Ubuntu Live  
C) SLAX  
D) Debian Live  

---

**93.** BackTrack was created by merging which two distributions?

A) WHoppiX and Debian  
B) Auditor Security Linux and WHAX  
C) WHAX and Knoppix  
D) Ubuntu Studio and WHAX  

---

**94.** OffSec was launched by Muts in which year?

A) 2004  
B) 2006  
C) 2008  
D) 2010  

---

**95.** Kali Linux's rolling release model means:

A) Users receive updates only at scheduled milestones  
B) Users are continuously up to date without needing full reinstalls  
C) The system reverts to a previous state after updates  
D) Packages are frozen after each release  

---

**96.** The `live-build` tool is used to:

A) Manage Debian packages  
B) Build custom Kali ISO images  
C) Configure the GRUB bootloader  
D) Compile the Linux kernel  

---

**97.** Where are the list of files installed by a package tracked in the dpkg database?

A) /var/lib/dpkg/info/*.list  
B) /etc/dpkg/lists/  
C) /usr/lib/dpkg/filelists/  
D) /var/cache/dpkg/  

---

**98.** The `apt-cache search` command is used to:

A) Install packages  
B) Search package descriptions for keywords  
C) Update the package index  
D) Remove packages  

---

**99.** To update the local package index in APT:

A) apt refresh  
B) apt update  
C) apt sync  
D) apt-get reindex  

---

**100.** The sources.list file (or files in sources.list.d/) defines:

A) Installed package configurations  
B) APT repositories to fetch packages from  
C) Kernel modules to load  
D) System startup services  

---

**101.** The command `uname -r` returns:

A) The CPU architecture  
B) The currently running kernel version  
C) The distribution name  
D) The hostname  

---

**102.** To enable IP forwarding immediately (without rebooting) on Linux:

A) echo 1 > /proc/sys/net/ipv4/ip_forward  
B) sysctl net.ipv4.ip_forward on  
C) iptables -t nat -j FORWARD  
D) ifconfig eth0 forward 1  

---

**103.** To make IP forwarding persistent across reboots, you edit:

A) /etc/network/interfaces  
B) /etc/sysctl.conf  
C) /etc/rc.network  
D) /proc/sys/net/net.conf  

---

**104.** The iptables rule `-t nat -A POSTROUTING -o eth0 -j MASQUERADE` does what?

A) Blocks all outgoing traffic on eth0  
B) Masks private client IPs with the external IP when packets leave eth0  
C) Forwards all traffic to wlan0  
D) Drops all FORWARD chain packets  

---

**105.** The dnsmasq configuration option `dhcp-range=172.24.1.50,172.24.1.150,12h` means:

A) Assign IP addresses from .50 to .150 with 12-day leases  
B) Assign IP addresses from .50 to .150 with 12-hour leases  
C) Block DHCP requests from .50 to .150  
D) Reserve .50 through .150 for static assignment  

---

**106.** To save iptables rules to a file:

A) iptables --save > /etc/iptables.rules  
B) iptables-save | sudo tee /etc/iptables.ipv4.nat  
C) iptables -S > /etc/iptables.ipv4.nat  
D) service iptables save  

---

**107.** To restore iptables rules from a file at boot via `/etc/rc.local`:

A) iptables --restore < /etc/iptables.ipv4.nat  
B) iptables-restore < /etc/iptables.ipv4.nat  
C) ip rule restore /etc/iptables.ipv4.nat  
D) iptables-load /etc/iptables.ipv4.nat  

---

**108.** The `hostapd` daemon is used for:

A) DNS caching  
B) DHCP serving  
C) IEEE 802.11 access point management  
D) Firewall management  

---

**109.** In the hostapd configuration, `DAEMON_CONF` should point to:

A) /etc/network/hostapd  
B) /etc/hostapd/hostapd.conf  
C) /var/run/hostapd.pid  
D) /usr/share/hostapd/default.conf  

---

**110.** The `dnsmasq` service provides:

A) Firewall and NAT services  
B) Lightweight DHCP and DNS caching  
C) Wireless injection support  
D) SSH tunneling  

---

**111.** To check if a systemd service is running:

A) service status servicename  
B) sudo systemctl status servicename  
C) ps aux | grep servicename  
D) init.d servicename status  

---

**112.** To make a service start automatically on every reboot:

A) sudo systemctl reload servicename  
B) sudo systemctl enable servicename  
C) sudo systemctl start servicename  
D) sudo systemctl boot servicename  

---

**113.** The `/etc/rc.local` file is made executable with which permission set?

A) 644  
B) 600  
C) 755  
D) 711  

---

**114.** Which man page section covers library functions (provided by system libraries)?

A) Section 1  
B) Section 2  
C) Section 3  
D) Section 4  

---

**115.** Man page section 5 covers:

A) Games  
B) Kernel routines  
C) Configuration files  
D) System administration commands  

---

**116.** To view man page section 2 for the `read` system call:

A) man read 2  
B) man 2 read  
C) man -s 2 read  
D) man read --section=2  

---

**117.** The `kali-linux-core` metapackage is:

A) The largest Kali metapackage  
B) The base system pulled by all other metapackages  
C) The wireless assessment metapackage  
D) The forensics metapackage  

---

**118.** Which metapackage contains GPU-powered tools?

A) kali-tools-hardware  
B) kali-tools-rfid  
C) kali-tools-gpu  
D) kali-tools-sdr  

---

**119.** The `kali-tools-wireless` metapackage covers:

A) Only 802.11 tools  
B) 802.11, Bluetooth, RFID, and SDR tools  
C) Only Bluetooth tools  
D) SDR tools only  

---

**120.** The `kali-tools-web` metapackage contains:

A) Password cracking tools  
B) Web application assessment tools  
C) Social engineering tools  
D) Wireless tools  

---

**121.** Which tool is described as "the most popular penetration testing tool" you need to know if serious about penetration testing?

A) Nmap  
B) Burp Suite  
C) Metasploit  
D) Aircrack-ng  

---

**122.** The OSCP certification exam is described as:

A) A multiple-choice exam lasting 3 hours  
B) A 24-hour hands-on penetration test in an isolated VPN network  
C) A take-home written assignment  
D) An online quiz with automatic grading  

---

**123.** A "file collision" error during dpkg installation occurs when:

A) The package is corrupted  
B) A file already exists from another installed package  
C) The disk is full  
D) Dependencies are missing  

---

**124.** The `--force-overwrite` option in dpkg should be used:

A) In all upgrades  
B) Only in exceptional situations to override packaging rules  
C) When downgrading packages  
D) Never — it is not a valid option  

---

**125.** To list all packages starting with 'b' using dpkg:

A) dpkg -l b*  
B) dpkg --list 'b*'  
C) dpkg -q b*  
D) dpkg --show b*  

---

**126.** The `fakeroot` package allows:

A) Kernel compilation with fake hardware  
B) Creating Debian packages without needing administrative privileges  
C) Running Kali as a non-root user with root permissions  
D) Faking a network interface  

---

**127.** The `debuild` command runs `dpkg-buildpackage` and also invokes:

A) dh_make  
B) lintian  
C) reprepro  
D) dpkg-sig  

---

**128.** `lintian` is used to:

A) Build Debian packages  
B) Validate a generated package against the Debian policy  
C) Sign Debian packages  
D) Install Debian packages  

---

**129.** To build a Debian source package from scratch, which tool generates initial packaging structure?

A) dpkg-buildpackage  
B) dh_make  
C) debuild  
D) reprepro  

---

**130.** `dpkg-buildpackage -us -uc` skips:

A) Installing the package  
B) Running lintian  
C) Signing the source package and changes file  
D) Building the binary package  

---

**131.** The four types of security assessments described in the course are:

A) Network, Application, Physical, Social  
B) Vulnerability assessment, Compliance test, Penetration test, Application assessment  
C) Red team, Blue team, Purple team, White team  
D) Internal, External, Remote, On-site  

---

**132.** A vulnerability assessment differs from a penetration test in that:

A) It is always automated  
B) It typically identifies and reports vulnerabilities without exploiting them  
C) It only tests web applications  
D) It requires physical access  

---

**133.** In the course, "risk" is best defined as:

A) The flaw in the system  
B) The attacker's capability  
C) What you are trying to prevent from happening  
D) The control mechanism  

---

**134.** Which vulnerability type allows an attacker to read local files from a web server's filesystem?

A) SQL Injection  
B) Buffer Overflow  
C) Local File Inclusion  
D) Race Condition  

---

**135.** When a SQL injection grants full interactive shell access to the host OS, it impacts:

A) Confidentiality only  
B) Integrity only  
C) Availability only  
D) Confidentiality, Integrity, and Availability  

---

**136.** Which command installs the prerequisites for building custom Kali ISO images?

A) apt install git make build-essential  
B) apt install curl git live-build  
C) apt install live-cd-tools git  
D) apt install iso-builder curl  

---

**137.** The `./build.sh --verbose` command in the live-build-config repository:

A) Builds a KDE-specific image  
B) Builds an updated but unmodified Kali live ISO  
C) Only downloads packages without building  
D) Validates existing ISO checksums  

---

**138.** Custom .deb files can be included in a live image by placing them in:

A) packages.chroot  
B) includes.chroot  
C) hooks/live/  
D) package-lists/  

---

**139.** The Kali package tracker allows you to:

A) Track your own package installations  
B) Follow the evolution of each package in Kali  
C) Submit bug reports to Debian  
D) Download tool source code  

---

**140.** The Kali signing process involves:

A) Uploading unsigned binary packages  
B) Uploading signed source packages built on dedicated build daemons  
C) Only checksumming packages  
D) Distributing unsigned repositories  

---

**141.** The `preseed` feature in Kali installation allows:

A) Pre-selecting a desktop environment  
B) Fully automated unattended installations  
C) Only pre-filling the timezone  
D) Skipping partitioning  

---

**142.** Kali Linux supports installation on Raspberry Pi because:

A) It provides x86 binaries compatible with Pi  
B) It provides ARM architecture images  
C) The Pi can emulate x86  
D) Debian provides official Pi support only  

---

**143.** The `net.ipv4.ip_forward = 1` setting in `/etc/sysctl.conf`:

A) Enables DHCP  
B) Permanently enables IPv4 packet forwarding between interfaces  
C) Disables the firewall  
D) Sets a static IP  

---

**144.** Which iptables target masks a private client IP with the external IP of the gateway?

A) DNAT  
B) SNAT  
C) MASQUERADE  
D) REDIRECT  

---

**145.** The `reprepro` tool is used for:

A) Recompiling packages  
B) Managing a Debian package repository  
C) Generating GPG keys  
D) Building live ISO images  

---

**146.** To export and add a GPG public key to APT's trusted keys:

A) gpg2 --export --armor email | apt-key add -  
B) gpg --trust email | apt-get key add  
C) apt-key import email.asc  
D) gpg --import-apt email  

---

**147.** Where should `sources.list` entries for custom repositories be added?

A) /etc/apt/sources.list directly  
B) /etc/apt/sources.list.d/ as a separate .list file  
C) /var/apt/sources/  
D) /usr/share/apt/repos/  

---

**148.** A GnuPG key used to sign an internal package repository should be placed in:

A) /etc/apt/trusted.gpg.d/  
B) /etc/apt/keys/  
C) /usr/share/apt/keys/  
D) /var/lib/apt/keys/  

---

**149.** The `SaltStack` configuration file for indicating the salt master location is placed in:

A) /etc/salt/master.d/  
B) /etc/salt/minion.d/  
C) /usr/share/salt/  
D) /var/lib/salt/  

---

**150.** The gsettings override file priority for organization-level overrides should be set to:

A) 50  
B) 70  
C) 80  
D) 90  

---

**151.** Which desktop environment directories are available in the default kali-config for live-build?

A) gnome, kde, xfce, mate, lxde, i3wm, e17  
B) gnome, kde, unity, cinnamon  
C) xfce only  
D) gnome and kde only  

---

**152.** The `--variant` option in `build.sh` specifies:

A) The kernel version  
B) The desktop environment or configuration variant  
C) The target architecture  
D) The number of CPU cores to use  

---

**153.** The `kali-config/common` directory in the live-build repo contains:

A) Architecture-specific configs  
B) Configuration shared by all variants  
C) Desktop environment icons only  
D) Package lists only  

---

**154.** The `package-lists/*.list.chroot` files in live-build define:

A) Packages to exclude from the image  
B) Packages to install in the live image  
C) Packages to download but not install  
D) Source packages to compile  

---

**155.** A metapackage in Debian/Kali is:

A) A package containing compiled binaries  
B) An empty package that depends on many other packages  
C) A package for managing other packages  
D) A source-only package  

---

**156.** The `kali-linux-headless` metapackage includes:

A) Default command-line tools only (no GUI)  
B) All graphical tools  
C) Only wireless tools  
D) The minimal base system  

---

**157.** Which boot menu key allows you to edit boot command line options in Kali?

A) F2  
B) Enter  
C) Tab  
D) Escape  

---

**158.** In graphical installation mode, how can you navigate between fields?

A) Only with Tab key  
B) With the mouse as you normally would on a graphical desktop  
C) With arrow keys only  
D) By typing field numbers  

---

**159.** Which step during Kali installation must succeed for network installations from a mini.iso?

A) Language selection  
B) Detecting network hardware  
C) Configuring the clock  
D) Partitioning  

---

**160.** During manual partitioning with an MSDOS partition table, logical partitions reside in:

A) The primary partition  
B) An extended partition  
C) A swap partition  
D) An LVM volume  

---

**161.** What is the purpose of a swap partition in Linux?

A) To store temporary files during installation  
B) To hold inactive RAM contents when physical RAM is insufficient  
C) To boot the kernel  
D) To store system logs  

---

**162.** The "All files in one partition" guided scheme actually creates how many partitions?

A) One  
B) Two (root and swap)  
C) Three (root, home, swap)  
D) Four  

---

**163.** Which command recompiles a Debian package from source code?

A) apt source package  
B) dpkg-buildpackage  
C) apt rebuild package  
D) make package  

---

**164.** `devscripts` is a suite of tools that includes:

A) lintian  
B) debuild  
C) reprepro  
D) live-build  

---

**165.** Which filesystem hierarchy directory is traditionally intended to contain user data?

A) /var/  
B) /home/  
C) /srv/  
D) /opt/  

---

**166.** The Video for Linux (V4L) interface is an example of:

A) A block device  
B) A kernel abstraction layer for hardware  
C) A user-space networking interface  
D) A character device file  

---

**167.** The `/dev/sda` file represents:

A) A character device for serial communication  
B) A block device representing a disk drive  
C) A virtual memory file  
D) A network interface  

---

**168.** The `/dev/input/mouse0` file is which type of device?

A) Block  
B) Character  
C) Socket  
D) Named pipe  

---

**169.** The Linux kernel's role is best compared to:

A) A musician  
B) The conductor of an orchestra  
C) The audience  
D) A stage  

---

**170.** The `BIOS/UEFI` starts which component first during boot?

A) The kernel  
B) The bootloader  
C) The init system  
D) The shell  

---

**171.** Which tool is used to search for and install packages from Kali's repositories interactively with conflict resolution?

A) dpkg  
B) apt  
C) aptitude  
D) All of the above  

---

**172.** DKMS packages typically require which kernel package to be installed?

A) linux-firmware  
B) linux-libc-dev  
C) linux-headers  
D) linux-image-dbg  

---

**173.** The `Kali ISO of Doom` and `Kali Evil Wireless Access Point` are examples of:

A) Official Kali installer variants  
B) Custom-built Kali Linux implementations  
C) Kali Live persistence modes  
D) Kali ARM images  

---

**174.** The live-build binary hooks can modify:

A) The live filesystem after it has been generated  
B) The content of the ISO image at the end of the build  
C) Installed packages inside the chroot  
D) The kernel configuration  

---

**175.** The default isolinux configuration changes made by Kali in binary hooks include:

A) Removing persistence menu entries  
B) Adding boot menu entries enabling persistence  
C) Replacing the bootloader with GRUB  
D) Setting the default kernel version  

---

**176.** Which tool is run by `debuild` to validate a package against Debian policy?

A) dh_make  
B) dpkg-sig  
C) lintian  
D) reprepro  

---

**177.** The KLCP certification full name is:

A) Kali Linux Certified Practitioner  
B) Kali Linux Certified Professional  
C) Kali Linux Core Professional  
D) Kali Linux Competency Program  

---

**178.** In the `dnsmasq.conf` for the Kali Pi AP, `server=8.8.8.8` means:

A) Use 8.8.8.8 as the DHCP server  
B) Forward DNS requests to Google's DNS  
C) Block DNS requests from 8.8.8.8  
D) Listen for DNS on 8.8.8.8  

---

**179.** The `bind-interfaces` option in dnsmasq:

A) Binds dnsmasq to all interfaces  
B) Ensures dnsmasq binds only to the specified interface  
C) Disables DHCP on all interfaces  
D) Enables DNS forwarding  

---

**180.** `domain-needed` in dnsmasq configuration means:

A) A domain name is required for DHCP  
B) Short names (without a domain) are not forwarded to upstream DNS  
C) Only .local domains are resolved  
D) DNS requests require authentication  

---

**181.** The `kali-tools-top10` metapackage contains:

A) All Kali tools  
B) The ten most popular tools  
C) Only web assessment tools  
D) GPU tools  

---

**182.** The `kali-tools-passwords` metapackage is focused on:

A) Password managers  
B) Password generation  
C) Password cracking tools  
D) Password policy enforcement  

---

**183.** To revert all manual partitioning changes without writing to disk:

A) Select "Cancel" at the partition screen  
B) Select "Undo changes to partitions"  
C) Reboot the installer  
D) Run `fdisk -u`  

---

**184.** The `make gconfig` kernel configuration interface depends on:

A) libqt4-dev  
B) libncurses5-dev  
C) libglade2-dev and libgtk2.0-dev  
D) libx11-dev  

---

**185.** The `make olddefconfig` command:

A) Asks interactively about each new config option  
B) Uses the default answer for all new configuration options  
C) Removes all new config options  
D) Resets the config to factory defaults  

---

**186.** Which file in /boot contains the kernel configuration used by the running kernel?

A) /boot/vmlinuz  
B) /boot/initrd.img  
C) /boot/config-$(uname -r)  
D) /boot/grub/grub.cfg  

---

**187.** The `bc` package installed as a dependency of `linux-source` is used for:

A) Boot configuration  
B) Arbitrary-precision arithmetic in build scripts  
C) Byte conversion  
D) Binary caching  

---

**188.** USB Encrypted Persistence in Kali adds which protection?

A) Requires root password to boot  
B) Encrypts the persistent partition on the USB drive  
C) Prevents booting in live mode  
D) Enables TPM-based unlock  

---

**189.** Kali Linux's forensics boot mode is best accessed via:

A) A live USB only  
B) The Kali boot menu  
C) Running `forensics-mode` after login  
D) Installing the kali-forensics package  

---

**190.** The command `apt-cache search ^linux-source` lists:

A) Available Linux kernel source packages  
B) Installed kernel modules  
C) Running kernel versions  
D) Kernel header packages  

---

**191.** Running `./build.sh --verbose` without `--variant` uses which variant by default?

A) gnome  
B) xfce  
C) default  
D) minimal  

---

**192.** In `dpkg -l` output, the status column `ii` means:

A) Package is installed with errors  
B) Package is installed correctly  
C) Package is not installed  
D) Package has config files only  

---

**193.** The Kali Linux rolling release is based on which Debian branch?

A) Debian Stable  
B) Debian Oldstable  
C) Debian Testing  
D) Debian Experimental  

---

**194.** The `/proc/sys/net/ipv4/ip_forward` file contains `0` by default, meaning:

A) IPv4 forwarding is enabled  
B) IPv4 forwarding is disabled  
C) DHCP is disabled  
D) NAT is active  

---

**195.** `apt -t kali-rolling upgrade` means:

A) Upgrade only kali-rolling packages  
B) Use kali-rolling as the target release when searching for upgrades  
C) Downgrade to kali-rolling  
D) Add kali-rolling to sources.list  

---

**196.** `make oldnoconfig` differs from `make oldconfig` in that it:

A) Asks questions for each new option  
B) Assumes a negative (no) answer to all new options  
C) Uses the default answer to all new options  
D) Runs interactively in a TUI  

---

**197.** In Kali Linux, all installed firmware — including closed-source firmware from Debian's non-free section — is installed because:

A) Kali is not bound by Debian's free software guidelines  
B) It ensures broad hardware support for security assessments  
C) Closed-source firmware is open to modification  
D) It is required by the Debian policy  

---

**198.** The Kali Live System documentation mentions that changes in default mode are:

A) Persistent across reboots  
B) Lost when you reboot  
C) Saved to an encrypted partition  
D) Backed up to the cloud  

---

**199.** Which command checks which version of the currently running kernel is in use?

A) cat /proc/version  
B) uname -r  
C) kernel --version  
D) Both A and B  

---

**200.** The Kali Linux Revealed course is licensed under:

A) GPL v3  
B) MIT License  
C) Creative Commons Attribution-ShareAlike 3.0 Unported  
D) Apache License 2.0  

---
---

## Answer Key

| Q | A | Q | A | Q | A | Q | A |
|---|---|---|---|---|---|---|---|
| 1 | C | 51 | B | 101 | B | 151 | A |
| 2 | C | 52 | C | 102 | A | 152 | B |
| 3 | C | 53 | B | 103 | B | 153 | B |
| 4 | C | 54 | B | 104 | B | 154 | B |
| 5 | C | 55 | B | 105 | B | 155 | B |
| 6 | B | 56 | B | 106 | B | 156 | A |
| 7 | C | 57 | B | 107 | B | 157 | C |
| 8 | D | 58 | B | 108 | C | 158 | B |
| 9 | B | 59 | C | 109 | B | 159 | B |
| 10 | B | 60 | B | 110 | B | 160 | B |
| 11 | D | 61 | B | 111 | B | 161 | B |
| 12 | B | 62 | B | 112 | B | 162 | B |
| 13 | B | 63 | C | 113 | D | 163 | B |
| 14 | C | 64 | B | 114 | C | 164 | B |
| 15 | C | 65 | B | 115 | C | 165 | B |
| 16 | B | 66 | B | 116 | B | 166 | B |
| 17 | C | 67 | A | 117 | B | 167 | B |
| 18 | B | 68 | B | 118 | C | 168 | B |
| 19 | C | 69 | B | 119 | B | 169 | B |
| 20 | C | 70 | C | 120 | B | 170 | B |
| 21 | D | 71 | C | 121 | C | 171 | D |
| 22 | C | 72 | B | 122 | B | 172 | C |
| 23 | B | 73 | B | 123 | B | 173 | B |
| 24 | C | 74 | B | 124 | B | 174 | B |
| 25 | B | 75 | C | 125 | B | 175 | B |
| 26 | C | 76 | B | 126 | B | 176 | C |
| 27 | C | 77 | C | 127 | B | 177 | B |
| 28 | B | 78 | B | 128 | B | 178 | B |
| 29 | C | 79 | C | 129 | B | 179 | B |
| 30 | B | 80 | B | 130 | C | 180 | B |
| 31 | C | 81 | C | 131 | B | 181 | B |
| 32 | B | 82 | C | 132 | B | 182 | C |
| 33 | B | 83 | C | 133 | C | 183 | B |
| 34 | B | 84 | B | 134 | C | 184 | C |
| 35 | B | 85 | B | 135 | D | 185 | B |
| 36 | B | 86 | D | 136 | B | 186 | C |
| 37 | B | 87 | B | 137 | B | 187 | B |
| 38 | B | 88 | B | 138 | A | 188 | B |
| 39 | B | 89 | C | 139 | B | 189 | B |
| 40 | C | 90 | B | 140 | B | 190 | A |
| 41 | B | 91 | B | 141 | B | 191 | C |
| 42 | B | 92 | C | 142 | B | 192 | B |
| 43 | B | 93 | B | 143 | B | 193 | C |
| 44 | B | 94 | B | 144 | C | 194 | B |
| 45 | C | 95 | B | 145 | B | 195 | B |
| 46 | C | 96 | B | 146 | A | 196 | B |
| 47 | B | 97 | A | 147 | B | 197 | B |
| 48 | C | 98 | B | 148 | A | 198 | B |
| 49 | B | 99 | B | 149 | B | 199 | D |
| 50 | B | 100 | B | 150 | D | 200 | C |
